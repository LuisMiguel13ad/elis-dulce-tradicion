---
phase: 03-dashboard-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/BakerStation.tsx
  - src/components/kitchen/BakerTicketCard.tsx
  - src/components/dashboard/DashboardLayout.tsx
  - src/components/dashboard/FrontDeskLayout.tsx
  - src/components/dev/DevTools.tsx
  - src/components/dashboard/DashboardHeader.tsx
  - src/test-orders.ts
  - src/utils/generateTestData.ts
autonomous: true

must_haves:
  truths:
    - "No dead code remains in the codebase"
    - "App builds without errors after cleanup"
    - "No broken imports or references"
  artifacts:
    - path: "src/components/dashboard/DashboardHeader.tsx"
      provides: "Clean header without test data button"
      contains: "DashboardHeader"
  key_links:
    - from: "src/App.tsx"
      to: "routes"
      via: "No BakerStation route exists"
      pattern: "no match expected"
---

<objective>
Remove all dead code, unused components, and dev utilities from the codebase.

Purpose: Clean production codebase by removing BakerStation page, unused layout wrappers, dev components, and test data utilities per CONTEXT.md decisions.
Output: Cleaner codebase with no unused files or dev-only functionality.
</objective>

<execution_context>
@/Users/luismiguel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luismiguel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-verification/CONTEXT.md
@.planning/phases/03-dashboard-verification/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove BakerStation page and related components</name>
  <files>
    src/pages/BakerStation.tsx
    src/components/kitchen/BakerTicketCard.tsx
    src/components/dashboard/FrontDeskLayout.tsx
  </files>
  <action>
    Delete the following files:
    1. `src/pages/BakerStation.tsx` - The redundant baker view page
    2. `src/components/kitchen/BakerTicketCard.tsx` - Only used by BakerStation
    3. `src/components/dashboard/FrontDeskLayout.tsx` - Only used by BakerStation

    These files are safe to delete because:
    - BakerStation is NOT in App.tsx routes (already removed or never added)
    - FrontDeskLayout is only imported by BakerStation
    - BakerTicketCard is only imported by BakerStation
    - Front Desk page (src/pages/FrontDesk.tsx) uses its own layout, not FrontDeskLayout
  </action>
  <verify>
    Run `npm run build` - should complete without errors
    Run `grep -r "BakerStation\|BakerTicketCard\|FrontDeskLayout" src/` - should return no results
  </verify>
  <done>
    BakerStation.tsx, BakerTicketCard.tsx, and FrontDeskLayout.tsx are deleted. Build succeeds. No imports reference these files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove unused layout and dev components</name>
  <files>
    src/components/dashboard/DashboardLayout.tsx
    src/components/dev/DevTools.tsx
  </files>
  <action>
    Delete the following files:
    1. `src/components/dashboard/DashboardLayout.tsx` - Unused wrapper component (not imported anywhere after BakerStation removal)
    2. `src/components/dev/DevTools.tsx` - Dev-only test utilities
    3. Delete the entire `src/components/dev/` folder if empty after DevTools removal

    Before deleting DashboardLayout, verify it has no imports:
    - Run `grep -r "DashboardLayout" src/` to confirm only self-reference remains

    The DevTools component:
    - Imports from test-orders.ts which we'll handle in next task
    - Only provides test data seeding functionality
    - Safe to remove for production
  </action>
  <verify>
    Run `npm run build` - should complete without errors
    Run `ls src/components/dev/` - folder should not exist or be empty
    Run `grep -r "DashboardLayout\|DevTools" src/` - should return no results
  </verify>
  <done>
    DashboardLayout.tsx deleted. DevTools.tsx and dev folder deleted. No broken imports. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove test data button and test utilities</name>
  <files>
    src/components/dashboard/DashboardHeader.tsx
    src/test-orders.ts
    src/utils/generateTestData.ts
  </files>
  <action>
    1. Edit `src/components/dashboard/DashboardHeader.tsx`:
       - Remove the import: `import { generateTestOrders } from '@/utils/generateTestData';`
       - Remove the state: `const [generating, setGenerating] = useState(false);`
       - Remove the handler: `handleGenerateData` function
       - Remove the "Generate Test Data" button (the Button component with Database icon)
       - Remove unused imports: `Loader2, Database` from lucide-react
       - Keep all other functionality intact (search, notifications, avatar)

    2. Delete `src/utils/generateTestData.ts` - Test data generation utility
       - First verify no other files import it (grep confirms only DashboardHeader uses it)

    3. Delete `src/test-orders.ts` - Test orders utility
       - Only imported by DevTools.tsx (already deleted)
       - Used by useOrderSearch.ts for mock updates, but that's just a listener comment, not active code

    4. After deleting test-orders.ts, check useOrderSearch.ts:
       - The comment mentions "Listen for mock updates (DevTools)" but the code just listens for localStorage events
       - No actual import of test-orders in useOrderSearch - safe to leave as-is

    Important: Keep the DashboardHeader functional - only remove the test data button and its dependencies.
  </action>
  <verify>
    Run `npm run build` - should complete without errors
    Run `grep -r "generateTestOrders\|generateTestData\|test-orders" src/` - should return no results or only benign comments
    Check DashboardHeader still renders: notifications, settings, avatar
  </verify>
  <done>
    DashboardHeader no longer has test data button. generateTestData.ts and test-orders.ts deleted. Build succeeds. Header still shows search, notifications, settings, and avatar.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build` succeeds with no errors
2. No references to deleted files exist in codebase
3. App starts without errors: `npm run dev`
4. Navigate to /owner-dashboard - loads without errors
5. Navigate to /front-desk - loads without errors
</verification>

<success_criteria>
1. All specified files deleted: BakerStation.tsx, BakerTicketCard.tsx, FrontDeskLayout.tsx, DashboardLayout.tsx, DevTools.tsx, generateTestData.ts, test-orders.ts
2. DashboardHeader.tsx cleaned of test data functionality
3. Build passes with zero errors
4. No console errors when loading Owner Dashboard or Front Desk
5. Grep for deleted component names returns no results
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-verification/03-01-SUMMARY.md`
</output>
