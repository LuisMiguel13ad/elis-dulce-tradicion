---
phase: 03-dashboard-verification
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/pages/OwnerDashboard.tsx
  - src/components/dashboard/OwnerSidebar.tsx
autonomous: true

must_haves:
  truths:
    - "Owner can access Products tab in dashboard"
    - "Owner can access Inventory tab in dashboard"
    - "Sidebar shows Products and Inventory menu items"
    - "MenuManager component loads and displays products"
    - "InventoryManager component loads and displays inventory"
  artifacts:
    - path: "src/pages/OwnerDashboard.tsx"
      provides: "Dashboard with Products and Inventory tabs"
      contains: "TabsContent value=\"products\""
    - path: "src/components/dashboard/OwnerSidebar.tsx"
      provides: "Sidebar with products and inventory menu items"
      contains: "products"
  key_links:
    - from: "src/pages/OwnerDashboard.tsx"
      to: "src/components/dashboard/MenuManager.tsx"
      via: "import and TabsContent"
      pattern: "import.*MenuManager"
    - from: "src/pages/OwnerDashboard.tsx"
      to: "src/components/dashboard/InventoryManager.tsx"
      via: "import and TabsContent"
      pattern: "import.*InventoryManager"
---

<objective>
Integrate MenuManager and InventoryManager components as new tabs in the Owner Dashboard.

Purpose: Make Products and Inventory management accessible from the Owner Dashboard per CONTEXT.md decisions, providing owners a single unified interface.
Output: Owner Dashboard with 2 new tabs (Products, Inventory) and corresponding sidebar navigation.
</objective>

<execution_context>
@/Users/luismiguel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luismiguel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-verification/CONTEXT.md
@.planning/phases/03-dashboard-verification/03-RESEARCH.md
@src/pages/OwnerDashboard.tsx
@src/components/dashboard/OwnerSidebar.tsx
@src/components/dashboard/MenuManager.tsx
@src/components/dashboard/InventoryManager.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Products and Inventory menu items to OwnerSidebar</name>
  <files>src/components/dashboard/OwnerSidebar.tsx</files>
  <action>
    Edit `src/components/dashboard/OwnerSidebar.tsx`:

    1. Add `Boxes` icon to the lucide-react import (for Inventory):
       ```typescript
       import {
         LayoutDashboard,
         ShoppingBag,
         Package,
         FileText,
         Settings,
         MessageSquare,
         LogOut,
         ChefHat,
         Calendar,
         Boxes,  // ADD THIS
       } from 'lucide-react';
       ```

    2. Update the menuItems array to include Products and Inventory:
       ```typescript
       const menuItems = [
         { id: 'overview', label: t('Resumen', 'Overview'), icon: LayoutDashboard },
         { id: 'orders', label: t('Pedidos', 'Orders'), icon: ShoppingBag },
         { id: 'calendar', label: t('Calendario', 'Calendar'), icon: Calendar },
         { id: 'products', label: t('Productos', 'Products'), icon: Package },  // ADD
         { id: 'inventory', label: t('Inventario', 'Inventory'), icon: Boxes },  // ADD
         { id: 'reports', label: t('Reportes', 'Reports'), icon: FileText },
       ];
       ```

    Note: Use `Package` for Products (already imported but unused in menu), `Boxes` for Inventory.
    Order: Overview, Orders, Calendar, Products, Inventory, Reports (logical grouping)
  </action>
  <verify>
    Run `npm run build` - no errors
    Run `grep -n "products\|inventory" src/components/dashboard/OwnerSidebar.tsx` - shows new menu items
  </verify>
  <done>
    OwnerSidebar shows 6 menu items including Products and Inventory. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Products and Inventory TabsContent to OwnerDashboard</name>
  <files>src/pages/OwnerDashboard.tsx</files>
  <action>
    Edit `src/pages/OwnerDashboard.tsx`:

    1. Add imports for MenuManager and InventoryManager at the top (after existing imports):
       ```typescript
       import MenuManager from '@/components/dashboard/MenuManager';
       import InventoryManager from '@/components/dashboard/InventoryManager';
       ```

    2. Add TabsContent for Products after the "reports" TabsContent (around line 428):
       ```tsx
       {/* --- TAB: PRODUCTS --- */}
       <TabsContent value="products" className="animate-in fade-in slide-in-from-bottom-5 duration-500">
         <MenuManager />
       </TabsContent>
       ```

    3. Add TabsContent for Inventory after Products:
       ```tsx
       {/* --- TAB: INVENTORY --- */}
       <TabsContent value="inventory" className="animate-in fade-in slide-in-from-bottom-5 duration-500">
         <InventoryManager />
       </TabsContent>
       ```

    4. Place these BEFORE the closing `</Tabs>` tag (around line 429).

    The pattern follows existing TabsContent structure:
    - Each tab has its own TabsContent with matching value
    - Animation classes for smooth transitions
    - Component rendered directly inside
  </action>
  <verify>
    Run `npm run build` - no errors
    Run `grep -n "TabsContent.*products\|TabsContent.*inventory" src/pages/OwnerDashboard.tsx` - shows both tab contents
    Run `grep -n "import.*MenuManager\|import.*InventoryManager" src/pages/OwnerDashboard.tsx` - shows both imports
  </verify>
  <done>
    OwnerDashboard imports MenuManager and InventoryManager. Two new TabsContent blocks exist for products and inventory. Build passes.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build` succeeds
2. `npm run dev` - start development server
3. Navigate to /owner-dashboard
4. Click "Products" in sidebar - MenuManager component renders
5. Click "Inventory" in sidebar - InventoryManager component renders
6. Both components load their data and display correctly
7. Tab switching works smoothly between all 6 tabs
</verification>

<success_criteria>
1. OwnerSidebar displays 6 menu items: Overview, Orders, Calendar, Products, Inventory, Reports
2. Clicking Products shows the MenuManager component with product table/list
3. Clicking Inventory shows the InventoryManager component with ingredient cards
4. Both components function correctly (can add/edit/delete products, update inventory)
5. All original tabs (Overview, Orders, Calendar, Reports) still work
6. Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-verification/03-02-SUMMARY.md`
</output>
